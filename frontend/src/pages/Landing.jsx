import { useState } from "react";
import { Link } from "react-router-dom";
import {
  FaBrain, FaChartLine, FaCheckCircle, FaDatabase, FaHeartbeat,
  FaLock, FaRobot, FaShieldAlt, FaStethoscope, FaUserMd, FaVials
} from "react-icons/fa";
import bahiaLogo from "../assets/bahia-ai-logo.jpg";
import { useAuth } from "../context/AuthContext.jsx";

const features = [
  {
    icon: FaBrain,
    title: "Four ML models",
    text: "KNN, SVM, Decision Tree and Random Forest are trained and benchmarked automatically — the best model by F1 + accuracy is selected for you."
  },
  {
    icon: FaChartLine,
    title: "Transparent metrics",
    text: "Accuracy, precision, recall, F1, ROC-AUC, confusion matrices and feature importance — everything is explainable, never a black box."
  },
  {
    icon: FaStethoscope,
    title: "Instant risk insights",
    text: "Enter tumor measurements and get an AI-assisted benign/malignant assessment with a confidence score in seconds."
  },
  {
    icon: FaDatabase,
    title: "Your data, your control",
    text: "Upload your own CSV datasets. Datasets, models and prediction history stay isolated to your account."
  },
  {
    icon: FaLock,
    title: "Secure by design",
    text: "JWT authentication, hashed passwords, rate limiting and security headers protect every request."
  },
  {
    icon: FaUserMd,
    title: "Built for clinicians",
    text: "A clean workflow designed with doctors and researchers: upload, train, evaluate, predict, review history."
  }
];

const steps = [
  { title: "Create your free account", text: "No credit card required. Start on the Free plan in under a minute." },
  { title: "Upload a dataset", text: "Bring your own CSV or start with the bundled breast cancer dataset." },
  { title: "Train & compare models", text: "One click trains all four models and ranks them by real metrics." },
  { title: "Predict & decide", text: "Get AI-assisted assessments with confidence scores to support clinical judgment." }
];

const faqs = [
  {
    q: "Is this a medical device?",
    a: "No. Bahia AI is a decision-support research tool. It does not diagnose cancer and must never replace professional medical evaluation, imaging, or biopsy."
  },
  {
    q: "Can I try it for free?",
    a: "Yes. Every new account gets one full AI risk prediction for free — no credit card required. After that, you buy credits only when you need them."
  },
  {
    q: "How do payments work?",
    a: "Buy a credit pack when you need more predictions. Credits never expire and are consumed only on successful predictions. Payments are in preview, so packs are currently granted free of charge."
  },
  {
    q: "Who can see my data?",
    a: "Only you. Your datasets, predictions and history are scoped to your account and protected by authentication on every endpoint."
  }
];

function Disclaimer() {
  return (
    <div className="rounded-xl border border-amber-300/60 bg-amber-50 p-4 text-sm leading-relaxed text-amber-900 dark:border-amber-500/30 dark:bg-amber-950/40 dark:text-amber-200">
      <p className="mb-1 flex items-center gap-2 font-bold"><FaHeartbeat /> Medical Disclaimer</p>
      <p>
        Bahia AI provides statistical estimates generated by machine-learning models trained on historical
        datasets. Results are <strong>not a diagnosis</strong> and must not be used as the sole basis for any
        medical decision. Always consult a qualified healthcare professional.
      </p>
    </div>
  );
}

export default function Landing() {
  const { token } = useAuth();
  const [openFaq, setOpenFaq] = useState(null);

  return (
    <div className="min-h-screen bg-gradient-to-b from-slate-50 via-white to-pink-50 dark:from-slate-950 dark:via-slate-900 dark:to-slate-950">
      <header className="mx-auto flex max-w-6xl items-center justify-between px-6 py-5">
        <div className="flex items-center gap-3">
          <img src={bahiaLogo} alt="Bahia AI" className="h-11 w-11 rounded-xl shadow-lg shadow-pink-500/20" />
          <div>
            <p className="text-lg font-bold">Bahia AI</p>
            <p className="text-xs text-slate-500 dark:text-slate-400">AI-assisted breast cancer screening insights</p>
          </div>
        </div>
        <nav className="flex items-center gap-3 text-sm font-semibold">
          <a href="#features" className="hidden hover:text-medical-blue sm:block">Features</a>
          <a href="#pricing" className="hidden hover:text-medical-blue sm:block">Pricing</a>
          {token ? (
            <Link to="/app" className="rounded-lg bg-medical-blue px-4 py-2 text-white shadow-lg shadow-blue-500/20">Open dashboard</Link>
          ) : (
            <>
              <Link to="/login" className="hover:text-medical-blue">Log in</Link>
              <Link to="/register" className="rounded-lg bg-medical-blue px-4 py-2 text-white shadow-lg shadow-blue-500/20">Start free</Link>
            </>
          )}
        </nav>
      </header>

      <section className="mx-auto max-w-6xl px-6 pb-16 pt-12 text-center">
        <span className="inline-flex items-center gap-2 rounded-full bg-pink-100 px-4 py-1.5 text-xs font-bold uppercase tracking-wide text-pink-600 dark:bg-pink-950/60 dark:text-pink-300">
          <FaShieldAlt /> Decision support · Not a diagnosis
        </span>
        <h1 className="mx-auto mt-6 max-w-3xl text-4xl font-extrabold leading-tight tracking-tight sm:text-5xl md:text-6xl">
          AI-powered insight for <span className="bg-gradient-to-r from-pink-500 to-medical-blue bg-clip-text text-transparent">earlier clarity</span> in breast cancer screening
        </h1>
        <p className="mx-auto mt-6 max-w-2xl text-lg text-slate-600 dark:text-slate-300">
          Train four machine-learning models on your data, compare them with transparent metrics, and get
          instant AI-assisted risk assessments — built for clinicians, researchers and health teams.
        </p>
        <div className="mt-8 flex flex-wrap justify-center gap-4">
          <Link to={token ? "/app" : "/register"} className="rounded-xl bg-medical-blue px-8 py-3.5 text-base font-bold text-white shadow-xl shadow-blue-500/30 transition hover:brightness-110">
            Start free — no card needed
          </Link>
          <a href="#how-it-works" className="rounded-xl border border-slate-300 bg-white/70 px-8 py-3.5 text-base font-bold transition hover:bg-white dark:border-slate-700 dark:bg-slate-900/70">
            See how it works
          </a>
        </div>
        <div className="mt-10 flex flex-wrap items-center justify-center gap-x-8 gap-y-3 text-sm font-semibold text-slate-500 dark:text-slate-400">
          <span className="flex items-center gap-2"><FaCheckCircle className="text-emerald-500" /> 4 models compared</span>
          <span className="flex items-center gap-2"><FaCheckCircle className="text-emerald-500" /> Full metric transparency</span>
          <span className="flex items-center gap-2"><FaCheckCircle className="text-emerald-500" /> Private per-account data</span>
        </div>
      </section>

      <section id="features" className="mx-auto max-w-6xl px-6 py-16">
        <h2 className="text-center text-3xl font-bold">Everything a screening workflow needs</h2>
        <div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
          {features.map(({ icon: Icon, title, text }) => (
            <div key={title} className="rounded-2xl border border-white/60 bg-white/80 p-6 shadow-sm backdrop-blur transition hover:shadow-lg dark:border-white/10 dark:bg-slate-900/70">
              <Icon className="text-2xl text-medical-blue" />
              <h3 className="mt-4 font-bold">{title}</h3>
              <p className="mt-2 text-sm leading-relaxed text-slate-600 dark:text-slate-300">{text}</p>
            </div>
          ))}
        </div>
      </section>

      <section id="how-it-works" className="mx-auto max-w-6xl px-6 py-16">
        <h2 className="text-center text-3xl font-bold">From CSV to confidence in four steps</h2>
        <div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
          {steps.map((step, index) => (
            <div key={step.title} className="relative rounded-2xl border border-white/60 bg-white/80 p-6 shadow-sm dark:border-white/10 dark:bg-slate-900/70">
              <span className="absolute -top-4 left-6 rounded-full bg-gradient-to-r from-pink-500 to-medical-blue px-3 py-1 text-xs font-bold text-white">{index + 1}</span>
              <FaVials className="mt-2 text-xl text-pink-500" />
              <h3 className="mt-3 font-bold">{step.title}</h3>
              <p className="mt-2 text-sm text-slate-600 dark:text-slate-300">{step.text}</p>
            </div>
          ))}
        </div>
      </section>

      <section id="pricing" className="mx-auto max-w-6xl px-6 py-16">
        <h2 className="text-center text-3xl font-bold">Your first prediction is free</h2>
        <p className="mx-auto mt-3 max-w-xl text-center text-slate-600 dark:text-slate-300">
          Every patient account starts with one free AI risk assessment.
          After that, buy credits only when you need them — no subscription required.
          Payments are in preview, so credits are currently granted free of charge.
        </p>
        <div className="mt-10 grid gap-6 sm:grid-cols-3">
          {[
            { label: "1 prediction", price: 9, popular: false },
            { label: "5 predictions", price: 39, popular: true },
            { label: "20 predictions", price: 129, popular: false }
          ].map((pack) => (
            <div
              key={pack.label}
              className={`relative flex flex-col rounded-2xl border p-7 shadow-sm ${
                pack.popular
                  ? "border-medical-blue shadow-xl shadow-blue-500/10 ring-2 ring-medical-blue"
                  : "border-white/60 bg-white/80 dark:border-white/10 dark:bg-slate-900/70"
              }`}
            >
              {pack.popular && (
                <span className="absolute -top-3 left-1/2 -translate-x-1/2 rounded-full bg-medical-blue px-4 py-1 text-xs font-bold text-white">Best value</span>
              )}
              <h3 className="font-bold">{pack.label}</h3>
              <p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Credits never expire</p>
              <p className="mt-4">
                <span className="text-4xl font-extrabold">${pack.price}</span>
              </p>
              <ul className="mt-5 flex-1 space-y-2.5 text-sm">
                <li className="flex items-start gap-2"><FaCheckCircle className="mt-0.5 shrink-0 text-emerald-500" /> Full AI risk assessment</li>
                <li className="flex items-start gap-2"><FaCheckCircle className="mt-0.5 shrink-0 text-emerald-500" /> Confidence score & recommendation</li>
                <li className="flex items-start gap-2"><FaCheckCircle className="mt-0.5 shrink-0 text-emerald-500" /> Saved to your personal history</li>
              </ul>
              <Link
                to={token ? "/app/billing" : "/register"}
                className={`mt-6 rounded-xl px-5 py-3 text-center font-bold transition ${
                  pack.popular
                    ? "bg-medical-blue text-white shadow-lg shadow-blue-500/25 hover:brightness-110"
                    : "border border-slate-300 hover:bg-slate-50 dark:border-slate-700 dark:hover:bg-slate-800"
                }`}
              >
                {token ? "Go to billing" : "Start with a free prediction"}
              </Link>
            </div>
          ))}
        </div>
        <p className="mt-8 text-center text-sm text-slate-500 dark:text-slate-400">
          Running a clinic or research team? Contact us about unlimited clinical plans.
        </p>
      </section>

      <section className="mx-auto max-w-3xl px-6 py-16">
        <h2 className="text-center text-3xl font-bold">Frequently asked questions</h2>
        <div className="mt-8 space-y-3">
          {faqs.map((faq, index) => (
            <button
              key={faq.q}
              type="button"
              onClick={() => setOpenFaq(openFaq === index ? null : index)}
              className="w-full rounded-xl border border-white/60 bg-white/80 p-5 text-left shadow-sm dark:border-white/10 dark:bg-slate-900/70"
            >
              <p className="flex items-center justify-between gap-4 font-bold">{faq.q}<span>{openFaq === index ? "−" : "+"}</span></p>
              {openFaq === index && <p className="mt-2 text-sm leading-relaxed text-slate-600 dark:text-slate-300">{faq.a}</p>}
            </button>
          ))}
        </div>
      </section>

      <section className="mx-auto max-w-4xl px-6 pb-16">
        <Disclaimer />
      </section>

      <footer className="border-t border-white/40 bg-white/60 py-8 dark:border-white/10 dark:bg-slate-950/60">
        <div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-4 px-6 text-sm text-slate-500 sm:flex-row dark:text-slate-400">
          <p>© {new Date().getFullYear()} Bahia AI. For research & decision support only.</p>
          <div className="flex gap-6">
            <a href="#pricing" className="hover:text-medical-blue">Pricing</a>
            <Link to="/login" className="hover:text-medical-blue">Log in</Link>
            <Link to="/register" className="hover:text-medical-blue">Register</Link>
          </div>
        </div>
      </footer>
    </div>
  );
}
